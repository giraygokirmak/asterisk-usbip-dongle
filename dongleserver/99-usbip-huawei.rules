# Ask systemd to reconcile only when a Huawei USB device appears. The periodic
# timer handles resets and missed events without blocking systemd-udevd.
ACTION=="add", SUBSYSTEM=="usb", ATTR{idVendor}=="12d1", TAG+="systemd", ENV{SYSTEMD_WANTS}+="usbip-huawei-bind.service"
